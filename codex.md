# Codex Notes
- Add to this note whenever we find a rule that needs to keep in this repo.
- Make sure design works at any display size. Avoid fixed-size layout fixes that only work for one viewport; prefer responsive constraints such as grid, flex, min/max widths, wrapping, and mobile-specific layouts.
